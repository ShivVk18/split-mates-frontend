# SplitMates Frontend Environment Variables

# The base URL of the backend API endpoints.
# By default in local development, Vite proxies requests from /api/v1 to http://localhost:4000,
# but you can configure it explicitly if backend is hosted on a different address.
VITE_API_BASE_URL="http://localhost:4000/api/v1"
